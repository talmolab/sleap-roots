# Tasks — add-circumnutation-tier3a-midline (Tier 3a midline)

Strict TDD: each scope unit ships a **failing test commit** (`test: … (TDD red)`)
then an **implementation commit** (`feat:/fix: … (TDD green)`). No bundling impl +
large test file into one commit (#222 retrospective). Fixup commits for review
findings ship a test alongside any substantive code change.

**Green-between-pairs rule.** After EVERY commit pair, the full suite stays green
and `black --check` / `pydocstyle` are clean (run the §11 quick gate). This is
load-bearing for the §3 ordering: the foundation migration is **atomic** with the
stub→impl transition so the foundation tests never go red.

## 0. Pre-flight
- [ ] 0.1 Confirm branch `add-circumnutation-tier3a-midline`; `uv sync`; baseline `uv run pytest tests/ -q` green.
- [ ] 0.2 Draft the **PR #8 tracking issue** in the vault (`c:\vaults\sleap-roots\circumnutation\`); show the user; do NOT post until OK (parent epic #197; labels `enhancement`, `circumnutation`, `multi-pr`). Do NOT file separate resampling/`L_gz` issues — those are owned by the already-roadmapped PR #9 / PR #10; record the deferral pointers in this PR's ADDED-requirement spec rationale.

## 1. `_noise.compute_sg_derivative` (commit pair — no foundation coupling, safe first)
- [ ] 1.1 RED: polynomial-exactness (`x = 2t²+3t+1`, window=5, order=3 → **deriv0 recovers `x` itself**, deriv1 = `4t+3`, deriv2 = `4` to machine precision); `delta` scaling (deriv1 with `delta=d` scales by `1/d`); short-input `len(x) < window` → `np.full(len(x), nan)`; window/order validation reuse (even window / order ≥ window / non-int → ValueError|TypeError naming the field); **deriv-range validation** (`deriv == polynomial_order` is ACCEPTED (inclusive upper bound); `deriv > polynomial_order` → ValueError naming `deriv`, NOT scipy's silent all-zeros; `deriv < 0` → ValueError, NOT scipy's opaque `factorial` error).
- [ ] 1.2 GREEN: implement `compute_sg_derivative(x, window, polynomial_order, deriv, delta=1.0, mode="interp")` in `_noise.py` next to `compute_sg_detrended` (reuse its validation block; add the `0 ≤ deriv ≤ polynomial_order` check). **scipy-import cleanup (same commit):** convert `_noise.py` to module-qualified `import scipy.signal`; update ALL existing bare `savgol_filter(` calls (5 call sites across 3 functions — `compute_sg_residual_xy` (x and y), `compute_msd_residual_xy` (x and y), `compute_sg_detrended`) to `scipy.signal.savgol_filter(`; drop `from scipy.signal import savgol_filter`. Because this edits the shared `_noise.py`, run the **full suite** for this pair's §11 gate (the consumers are `kinematics`, `qc`, `nutation`, `psi_g` — `kinematics` uses `compute_sg_residual_xy` for its growth-axis gate, so the qc/nutation/psi_g subset is NOT sufficient).

## 2. `_geometry.compute_path_curvature` (commit pair — no foundation coupling)
- [ ] 2.1 RED: **absolute sign anchor** — `compute_path_curvature([1.],[0.],[0.],[1.]) == +1.0` exactly; CCW unit circle `(cos t, sin t)` derivatives → `κ ≈ +1/R` (R=1 → +1); CW circle → `−1/R`; straight line (zero accel) → `κ ≈ 0`; `|v|=0` frame → `NaN` (no RuntimeWarning under `simplefilter("error")`); length-mismatched inputs → `ValueError`. **Cross-helper sign-consistency anchor** — on the same CCW loop, assert `np.sign(compute_path_curvature(...)) == +1` while `int(np.sign(compute_psi_g(x,y)[-1] − compute_psi_g(x,y)[0])) == −1`, i.e. `sign(κ) == −handedness` (the publication-trait-inversion guard).
- [ ] 2.2 GREEN: implement `compute_path_curvature(x_dot, y_dot, x_ddot, y_ddot)` in `_geometry.py` next to `compute_psi_g`/`compute_signed_area`; `errstate(divide,invalid,over)` + the helper returns the raw `(ẋÿ−ẏẍ)/|v|³` (the caller does the `~isfinite→NaN` sweep — but the helper itself NaN-s exact `|v|=0`). Google docstring with the **y-down disambiguation** anchored on the formula sign (mirror `compute_signed_area`'s y-down wording).

## 3. `MidlineResult` + `reconstruct` shell **+ foundation-test migration (ATOMIC pair)**
> The moment `reconstruct` stops raising `NotImplementedError`, the foundation
> tests (`test_stub_callable_raises_with_correct_pr` over `STUB_MODULES`,
> `test_stub_accepts_constants_kwarg` over `STUBS_WITH_CONSTANTS_KWARG`) would go
> red. The foundation-table migration and the first non-raising `reconstruct`
> MUST land in the SAME commit pair.
- [ ] 3.1 RED (one commit, two coordinated edits):
  - `tests/test_circumnutation_midline.py`: `reconstruct(x, y, 300.0)` on a small valid finite array returns a `MidlineResult`; the 7 per-frame arrays have length `n` and the documented dtypes (int64 `frame_indices`; float64 `x_smooth_px`/`y_smooth_px`/`speed_px_per_frame`/`arc_length_px`/`curvature_px_inv`; bool `velocity_sub_noise_mask`); the 6 scalars present with documented types; `arc_length_px[0] == 0.0` and monotonic non-decreasing.
  - `tests/test_circumnutation_foundation.py`: remove `("midline","reconstruct",8)` from `STUB_MODULES` (line ~50 → 4 remaining stubs) + `("midline","reconstruct")` from `STUBS_WITH_CONSTANTS_KWARG` (line ~850); add `("midline","reconstruct")` to `IMPLEMENTATIONS_WITH_CONSTANTS_KWARG` (line ~859) with a `# added by PR #8 (stub→impl)` trailing comment; add `"midline"` to the explicit `test_module_logger_is_namespaced` list (lines ~745–777); add an `elif module_name == "midline":` branch **after the `psi_g` elif (~line 937), before the `else:` (~line 967)** building **raw 1-D float64 arrays** (`n=8`, NOT a DataFrame — first array-typed impl branch) and calling `reconstruct(x, y, 300.0, constants=ConstantsT())` (the `cadence_s=300.0` positional is REQUIRED — don't write `fn(x, y, constants=...)`), asserting a `MidlineResult` is returned; update the `STUB_MODULES` block-comment (lines ~38–48) with a PR #8 paragraph (the four sub-moves + no `_CONSTANTS_VERSION` bump). Also assert the returned `MidlineResult`'s resolved `sg_window == 5`, `sg_degree == 3`, `noise_mask_k == 2` in the new `test_circumnutation_midline.py` fields test.
- [ ] 3.2 GREEN (one commit): define `MidlineResult` (frozen `attrs`) + the `reconstruct` shell — resolve constants/`sg_window`, validate inputs (raise), the degenerate gate (return before any `np.std`/`hypot`/`cumtrapz`), and the happy-path orchestration (SG deriv 0/1/2 → speed → `cumulative_trapezoid` → curvature via `_geometry` + `~isfinite→NaN` sweep → `σ_v=np.std(speed, ddof=0)` → mask → assemble). Suite GREEN after this pair.

## 4. Input-validation boundary (commit pair)
- [ ] 4.1 RED: `x`/`y` non-ndarray → TypeError; non-1-D / complex / object / non-numeric dtype → ValueError; `len(x) != len(y)` → ValueError; non-finite (NaN / ±inf) in `x` or `y` → ValueError (rejected, NOT dropped) and the **non-finite check precedes the length gate** (a `3 ≤ n < sg_window` all-NaN track RAISES, does not return graceful-NaN); bad `cadence_s` value→ValueError / type→TypeError (reuse `temporal_cwt._validate_cadence_s`, incl. `bool`/`np.bool_`/str rejection); `sg_window` even / `≤ SG_DEGREE` / non-int → ValueError|TypeError naming `sg_window`; `constants` wrong type → TypeError naming `constants`.
- [ ] 4.2 GREEN: wire the validators (reuse `compute_sg_derivative`'s window/order validation for `sg_window`; `_check_constants` for `constants`).

## 5. Arc length, speed, curvature numerics + cadence-independence (commit pair)
> §3.2 builds the **happy-path orchestration only** (SG → speed → arc → curvature → mask). The degenerate gate + raw-stationary detection land in §6.2. So §5.1 (numeric contracts on a non-degenerate input) and §6.1 (degenerate paths) are genuine RED-first against behavior §3.2/§6.2 add — NOT contract-locking-on-arrival. If during implementation §3.2 already includes the full gate, explicitly note §5/§6 as contract-locking pairs in the commit message.
- [ ] 5.1 RED: on a known analytic input (e.g. a circle radius R sampled densely), `arc_length_px[-1]` ≈ circumference fraction; `speed_px_per_frame ≈ √(ẋ²+ẏ²)`; interior `curvature_px_inv ≈ +1/R` (LOOSE `atol≈1e-3` — SG discretization, NOT the determinism floor); `velocity_sub_noise_mask` polarity (`True ⇔ speed ≤ NOISE_MASK_K·σ_v`) on a hand-built speed profile with a known slow frame; `sigma_v_px_per_frame == np.std(speed, ddof=0)`. **Cadence-independence** (spec scenario): `reconstruct(x, y, 300.0)` vs `reconstruct(x, y, 1.0)` → `arc_length_px`, `curvature_px_inv`, `speed_px_per_frame`, `velocity_sub_noise_mask` bit-identical at `atol=0`; only the `cadence_s` provenance scalar differs (guards against anyone multiplying speed by `cadence_s`).
- [ ] 5.2 GREEN: confirm the §3.2 orchestration satisfies these (no new product code expected beyond §3.2; this pair locks the numeric contracts).

## 6. Degenerate / edge cases (commit pair — all assert no `RuntimeWarning` via `simplefilter("error")`)
- [ ] 6.1 RED: **parametrize `n ∈ {0,1,2,3,4,5}` with `sg_window=5`** to pin the `<window`/`==window` transition (not just sample it) — `n ∈ {0..4}` → all-NaN length-`n` (`n=0` → length-0), `is_degenerate=True`, `sigma_v_px_per_frame` NaN, no `np.std([])` warning at `n=0`; `n == sg_window (5)` non-stationary → real reconstruction (`is_degenerate=False`; deriv=2 works at window=5/degree=3); **raw-stationary** (`x=y=const`, `n ≥ sg_window`) → all-NaN, `is_degenerate=True` (detected on the RAW input despite post-SG float dust); near-stationary + huge-accel adversarial input → curvature finite-or-NaN (NO inf, NO overflow warning).
- [ ] 6.2 GREEN: confirm the degenerate gate + `errstate(...,over=...)`+`~isfinite` sweep cover the table; trace every `n` from 0.

## 7. Determinism + canary + logging (commit pair)
- [ ] 7.1 RED: same-process `reconstruct` twice → all float arrays bit-identical at `atol=0`, `frame_indices`/`is_degenerate` exactly equal. **Two SEPARATE tolerances on the circle canary (R fixed, `n=128`):** (i) ORACLE — interior `curvature_px_inv ≈ 1/R` at a LOOSE physical `atol≈1e-3` (SG discretization limits this to ~1e-4, NOT 1e-9); (ii) REPRODUCIBILITY — matches a **hardcoded `_MIDLINE_CANARY_EXPECTED` array literal in the test file** (captured by a one-off `capture_midline_canary.py` run, mirroring `test_circumnutation_temporal_cwt.py`'s `_CANARY_EXPECTED_VALUES` pattern — the RED test must NOT read a not-yet-generated file) at `atol=1e-9, rtol=0`. Same hardcoded-literal + `atol=1e-9` for a synthetic-generator canary `generate_trajectory(random_state=0, n_frames=128, T_nutation_s=3333, cadence_s=300, noise_sigma_px=0.5)`. Logging — `caplog` at DEBUG: exactly one DEBUG record from `sleap_roots.circumnutation.midline`, message starts `"midline.reconstruct("`, contains `n_frames=` and `sg_window=`; no INFO/WARNING/ERROR.
- [ ] 7.2 GREEN: add `scripts/circumnutation/capture_midline_canary.py` (mirror `capture_temporal_cwt_canary.py`, with full Google docstrings) that PRINTS the circle + synthetic canary arrays for pasting into the test as `_MIDLINE_CANARY_EXPECTED`; add the single `logger.debug(...)` at the start of `reconstruct` (after validation, lazy %-formatting); confirm determinism (no random/ordering nondeterminism; `np.std` ddof=0; no argmax).

## 8. Real plate-001 + cross-tier validation (commit pair)
- [ ] 8.1 RED: a test-local loader (mirror `_load_proofread_track_df`/`_load_proofread_enriched`, `Path()`-based, with `@pytest.mark.skipif(not _FIXTURE.exists())` so LFS-less checkouts skip not fail) over the 6 proofread tracks of `plate_001_greyscale.tracked_proofread.slp` — per non-degenerate track: `arc_length_px` monotonic + `s[0]=0`; `curvature_px_inv` all-finite on `~velocity_sub_noise_mask` frames; **`max|curvature_px_inv[~mask]| < 1.0 px⁻¹`** (the unmasked array is the plausible one; observed ≈0.09–0.17, ~6× headroom); **mask fraction `0.1 < frac < 0.85`** recorded in the docstring/commit (observed ≈0.38–0.61 — widened upper bound for CI headroom; replaces the false "flags ~nothing" expectation; record that the fraction is data-SNR-dependent).
- [ ] 8.2 GREEN-phase: cross-tier acceptance — primary ROBUST invariant `arc_length_px[-1] ≤ L` per track (`L = Σ‖diff(xy)‖`, recovered/recomputed via `kinematics`; SG smoothing never lengthens the path — SNR-independent); SECONDARY magnitude tolerance `abs(arc_length_px[-1] − L)/L` ≤ ~5% (observed 2.1–3.1% on plate-001 — data-SNR-dependent: record the per-track `σ_pos` via `compute_sg_residual_xy` alongside the deviation so a noisier future fixture doesn't trip it mysteriously); the tolerance MUST clear a pre-committed floor. Document the gap-frame caveat (Tier 0 NaN-drops before summing; plate-001 has no gaps).

## 9. theory.md patch + docs
- [ ] 9.1 Patch `docs/circumnutation/theory.md` §6.2: add a y-down curvature-sign clarification ("`+κ` is a left turn in y-up math axes / clockwise-as-displayed in the y-down image frame; anchor on the formula sign") **AND the cross-helper note `sign(κ) == −handedness`** (the ψ_g family uses the swapped `atan2(dx, dy)`, so curvature and `handedness` have opposite as-displayed polarity — prevents a PR #9/#10 chirality-trait inversion). Verify the patch does NOT contradict the existing `_geometry.compute_psi_g`/`compute_signed_area` handedness convention prose. Add an Appendix B (Corrections) entry citing PR #8, **preserving** the original "positive = left turn" wording (do NOT silently overwrite).
- [ ] 9.2 `docs/changelog.md` entry; confirm `uv run mkdocs build` renders `midline.reconstruct`, `MidlineResult`, `_noise.compute_sg_derivative`, `_geometry.compute_path_curvature`.

## 10. Verification gates (must pass before requesting merge)
- [ ] 10.1 `uv run pytest tests/ -q` green; ≥ 84% project coverage; aim ≥ 90% on `midline.py` + the two new helpers (defensive branches `# pragma: no cover` with invariant comments).
- [ ] 10.2 `uv run black --check sleap_roots tests` clean.
- [ ] 10.3 `uv run pydocstyle --convention=google sleap_roots/circumnutation/` clean.
- [ ] 10.4 `uv lock --check` clean; `uv run mkdocs build` builds.
- [ ] 10.5 `npx openspec validate add-circumnutation-tier3a-midline --strict` valid.
- [ ] 10.6 CI matrix Ubuntu/Windows/macOS green on the final commit (cross-OS determinism for the savgol+cumtrapz stack at `atol=1e-9`).
- [ ] 10.7 `/copilot-review` + `/review-pr` findings reconciled or deferred to tracked issues.

## 11. Quick gate (run after EVERY commit pair, not just at the end)
- [ ] 11.x `uv run pytest tests/test_circumnutation_midline.py tests/test_circumnutation_foundation.py -q && uv run black --check sleap_roots tests && uv run pydocstyle --convention=google sleap_roots/circumnutation/` — catches the §3 atomicity hazard early. After the §1 `_noise` pair specifically (it edits the shared `_noise.py` + all 5 `savgol_filter(` call sites across 3 functions), run the **full suite** (`uv run pytest tests/ -q`) — the `_noise` consumers are `kinematics` (growth-axis gate), `qc`, `nutation`, `psi_g`, so the circumnutation subset alone is not sufficient.
