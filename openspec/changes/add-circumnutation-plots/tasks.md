# Tasks — add-circumnutation-plots (PR #16)

TDD throughout: write the RED test first, watch it fail for the right reason, implement to GREEN, refactor. The first commit that makes any `plotting` callable non-raising MUST be **atomic** with the foundation migration (Task 2) — otherwise `test_stub_callable_raises_with_correct_pr` (parametrized over `STUB_MODULES`) and the logger-namespace test go red.

## 1. Test scaffolding + Agg backend

- [ ] 1.1 Create `tests/test_circumnutation_plotting.py`. Add an autouse fixture forcing the headless backend: `matplotlib.use("Agg", force=True)` (RED first: assert `matplotlib.get_backend().lower() == "agg"`). Add small builders for valid `ScaleogramResult`/`SpatialScaleogramResult`/`RidgeResult`/`SpatialRidgeResult`/`MidlineResult` fixtures (reuse the patterns in `test_circumnutation_spatial_cwt.py` / the tier tests; do NOT hand-roll frozen-dataclass fields that drift from source).
- [ ] 1.2 Add an **autouse teardown fixture** `yield; plt.close("all")` so a structural test that fails before its own `plt.close(fig)` does NOT leak a figure into the next test's leak check (avoids misleading cascade failures). The `assert plt.get_fignums() == []` leak assertion lives only in the public-fn smoke tests (3.5/4.3/5.2), AFTER the public fn (which closes its own figure in a `finally`).

## 2. Foundation migration (ATOMIC with the first non-raising callable)

- [ ] 2.1 RED: write the new plotting callability tests in `test_circumnutation_foundation.py` (mirror the PR #15 `aggregation` callability scenario) — `plotting.scaleogram(valid_result, tmp_path/"s.png")` returns a `Path` and does not raise; `trail_overlay`/`plate_panel`/`save_plots` likewise. These fail while the stub still raises.
- [ ] 2.2 Remove `("plotting", "scaleogram", 16)` from `STUB_MODULES`; update the "Calling each remaining stub" test/comment to the 1 remaining stub (`parametric`); update the count-ledger comment block (foundation lines ~66-72) to record impl 11→12 / stub 2→1 (the counts live ONLY in that comment — no runtime count assertion). Add `plotting` to the explicit `test_module_logger_is_namespaced` list. Confirm `scaleogram` is NOT added to `IMPLEMENTATIONS_WITH_CONSTANTS_KWARG` (no `constants=` kwarg).
- [ ] 2.3 GREEN (**explicit atomic commit boundary**): ONE commit contains (a) the `STUB_MODULES` removal, (b) the logger-list addition, (c) the new callability tests (2.1), (d) a minimally-working `scaleogram` + `_build_scaleogram_figure` (from Task 3), AND (e) the module-docstring `L_gz`-marker drop (so no commit ships a graduated module whose docstring still advertises the unimplemented marker). The structural RED tests 3.1–3.4 (failing on `AttributeError`/`ImportError`, not touching `STUB_MODULES`) MAY be a prior commit. Run `uv run pytest tests/test_circumnutation_foundation.py -q` — green after the atomic commit. Note: `plotting.scaleogram` is called with NO valid args nowhere in the suite (the new callability test passes a valid result + path); a bare `scaleogram()` would raise `TypeError`, not `NotImplementedError`.

## 3. `scaleogram` (temporal + spatial, polymorphic)

- [ ] 3.1 RED (structural): `_build_scaleogram_figure(result)` returns a `matplotlib.figure.Figure`; for a temporal `ScaleogramResult` assert 2 axes (heatmap + colorbar), a `QuadMesh` artist, x-label `time [s]`, y-label `period [s]`, log y-scale, colorbar label `power |C|²`. For a `SpatialScaleogramResult` assert `arc length [px]` / `wavelength [px]`.
- [ ] 3.2 RED: `LogNorm` floor — a result with exact-zero power cells must not raise (assert no `ValueError`/warning); the norm `vmin` is the smallest positive power.
- [ ] 3.3 RED: COI dimming — assert the `coi_mask==True` region is visually de-emphasized (an overlay/alpha artist present); confirm polarity (True == dimmed == unreliable).
- [ ] 3.4 RED: ridge overlay — passing a matching `RidgeResult` adds a ridge line; segments inside COI (`in_coi==True`) are faded. Passing a MISMATCHED ridge type (spatial ridge + temporal scaleogram, or vice versa) raises `TypeError` naming both expected types; passing a JUNK `ridge_result` (e.g. `object()`, neither ridge type) ALSO raises `TypeError` (not `AttributeError`, G4); passing a non-result `scaleogram_result` raises `TypeError`.
- [ ] 3.4b RED: spatial faithfulness — for a `SpatialScaleogramResult`, the wavelength axis label marks it **uncalibrated convention px** (not the calibrated `lambda_spatial_*` trait), and spatial ridge positions with `in_coi==True` are faded just like the temporal ridge.
- [ ] 3.4c RED (G1, CC-3): `inspect.signature` of all four public fns contains no `px_per_mm`; no built figure's axis/colorbar label contains the substring `"mm"`; length labels use bracketed `[px]`/`[px⁻¹]` suffixes.
- [ ] 3.5 RED (smoke): public `scaleogram(result, out_path)` writes a non-empty PNG, returns `out_path`, closes the figure (`plt.get_fignums()==[]`).
- [ ] 3.6 GREEN: implement `_build_scaleogram_figure` + `scaleogram` (savefig + `plt.close` in `finally`; build the figure BEFORE any op that can raise, or wrap creation; return `out_path`). Module-level `_DPI`, `_FIGSIZE_SCALEOGRAM`, `_CMAP_SCALEOGRAM`. Decide + implement the COI-dimming mechanism (a chosen overlay/alpha artist) so 3.3 asserts that concrete artist, not an unspecified one.

## 4. `trail_overlay` (κ-color-coded, y-down)

- [ ] 4.1 RED (structural): `_build_trail_figure(midline_result)` returns a Figure with a `LineCollection` whose `get_array()` length == (n_points − 1) (per-segment, midpoint-averaged κ), a diverging colormap, a symmetric norm centered at 0 with limits ±(98th-pct |κ| over finite), y-axis inverted (image-down), equal aspect, colorbar `κ [px⁻¹]`.
- [ ] 4.2 RED: NaN-κ handling — a `MidlineResult` with NaN curvature segments renders them via a copied colormap's `set_bad`. Capture the global colormap's `get_bad()` (or registry entry) BEFORE the call and assert it is UNCHANGED after (catches an in-place `_CMAP_KAPPA.set_bad(...)` mutation), proving `cmap.copy()` is used.
- [ ] 4.2b RED: clipping visibility — the colorbar uses `extend="both"` (the symmetric ±98th-pct norm clips ~2% of |κ|). And (all-NaN-κ edge) a non-degenerate `MidlineResult` with entirely-NaN `curvature_px_inv` does NOT raise (skipped or default-limit norm), since `np.percentile([], 98)` is undefined.
- [ ] 4.3 RED (smoke): `trail_overlay(midline_result, out_path)` writes a non-empty PNG, returns `out_path`, closes the figure.
- [ ] 4.4 GREEN: implement `_build_trail_figure` + `trail_overlay`. Module-level `_FIGSIZE_TRAIL`, `_CMAP_KAPPA`, `_KAPPA_PCT = 98.0`. Factor the segment+norm helper so `plate_panel` reuses it.

## 5. `plate_panel` (2×3, shared norm + colorbar)

- [ ] 5.1 RED (structural): `_build_panel_figure(midline_results)` returns a Figure with 6 axes; every subplot uses the SAME κ norm (one norm computed across all plants' POOLED finite κ); exactly one shared colorbar (via an explicit `ScalarMappable(norm, cmap)`, `extend="both"`); empty cells hidden when < 6 results; deterministic cell→plant order (the `sort=False` groupby order).
- [ ] 5.1b RED (G7 + empty-pool): decide and test the >6-plants behavior (raise `ValueError` vs first-6 with a logged drop — pick one, document in design/spec); AND a collection whose entire pooled κ is non-finite (all plants all-NaN/degenerate) produces a defined figure (or skips) without a NaN-limit norm crash.
- [ ] 5.2 RED (smoke): `plate_panel(midline_results, out_path)` writes a non-empty PNG, returns `out_path`, closes the figure.
- [ ] 5.3 GREEN: implement `_build_panel_figure` + `plate_panel`. Module-level `_FIGSIZE_PANEL`.

## 6. `save_plots` orchestrator (re-derivation + plots/ + enabled + filenames)

- [ ] 6.1 RED: `save_plots(inputs, out_dir, enabled=False)` writes nothing, creates no `plots/`, returns `[]`, logs one INFO line.
- [ ] 6.2 RED: re-derivation fidelity — `save_plots` groups `inputs.trajectory_df` by `_IDENTITY_5_TUPLE` (`dropna=False, sort=False`, matching the tiers) and re-derives via the EXACT tier helpers: Tier 1 `_select_signal(group,"lateral")` (in `try/except ValueError`) → finite-any short-circuit → `_noise.compute_sg_detrended(raw, window=int(SG_WINDOW_DETREND), polynomial_order=int(SG_DEGREE))` → finite-any short-circuit → `try: temporal_cwt.compute_scaleogram(signal, cadence_s, constants) except ValueError` → `extract_ridge` → `smooth_ridge` (overlaid); Tier 3 finite-tip mask → `midline.reconstruct` → degeneracy gate → `spatial_cwt.resample_curvature` → degeneracy gate → `compute_scaleogram` → `extract_ridge`. Assert (e.g. via monkeypatched spies) the same helpers/kwargs are called — **patch at the binding `plotting` uses** (e.g. `sleap_roots.circumnutation.plotting.temporal_cwt.compute_scaleogram` if `plotting` calls `temporal_cwt.compute_scaleogram(...)` module-qualified; do NOT patch `temporal_cwt.compute_scaleogram` if `plotting` did `from ...temporal_cwt import compute_scaleogram`). Require `plotting` to call helpers **module-qualified** so the spy target is unambiguous (G6).
- [ ] 6.3 RED: filenames keyed on integer `track_id` (`plant{track_id}_scaleogram_temporal.png`, `..._scaleogram_spatial.png`, `..._trail.png`, `panel.png`); NOT on `plate_id`/`plant_id`. Assert a row with `NaN` plate_id/plant_id still yields unique `track_id`-keyed names.
- [ ] 6.4 RED: degenerate-plant skip — a plant whose chain hits a gate omits only its affected plot(s) (DEBUG-logged), others still render; return list reflects the subset. Use a NAMED deterministic fixture that trips the gate (e.g. a stationary/all-NaN track for the Tier 1 short-circuit, and a too-short/degenerate track for `mr.is_degenerate`) so the DEBUG-skip branch is covered deterministically, not incidentally.
- [ ] 6.5 RED (provenance, R-S4): `save_plots` writes `plots/plots_metadata.json` — assert it parses as strict JSON (no NaN/Infinity), and records `_CONSTANTS_VERSION`, the resolved display constants (`_KAPPA_PCT`, colormaps, `_DPI`, figsizes), the per-plant `_IDENTITY_5_TUPLE` tuples plotted, the written PNG filenames, and a relative back-reference to `run_metadata.json`. `enabled=False` writes no sidecar. Coerce numpy/`Path` to native (mirror sleap-roots-analyze `convert_to_json_serializable`; `json.dumps(..., allow_nan=False)`).
- [ ] 6.6 GREEN: implement `save_plots` (`plots/` via `mkdir(parents=True, exist_ok=True)`; the `plots_metadata.json` sidecar; INFO per-plate count, DEBUG per-plant). Reuse Tasks 3–5 renderers.

## 7. Real plate-001 integration test (the validation)

- [ ] 7.1 RED→GREEN — place this integration test in `tests/test_circumnutation_pipeline.py` (so it reuses the module-local `_load_plate001_inputs()` + `_PROOFREAD_FIXTURE` + the `@pytest.mark.skipif(not _PROOFREAD_FIXTURE.exists())` guard directly); if instead placed in `test_circumnutation_plotting.py`, it MUST copy the `_PROOFREAD_FIXTURE` path + skipif guard (an unguarded copy = CI red on machines without the Git-LFS file). Then: `save_plots(inputs, out/"out")` writes its PNG set into `out/"out"/plots/`. Assert the returned `list[Path]` count equals the self-consistent derived count `(#finite T_nutation_median) + (#finite traveling_wave_residual) + (#non-degenerate trails) + 1`, every file non-empty, no exception. Separately assert `enabled=False` produces no files and `[]`.
- [ ] 7.2 Confirm the temporal chain succeeds for the plate-001 plants (record the actual per-plant pass counts in a comment); if any plant is degenerate, the derived count handles it — do NOT hard-code 19. If reality diverges from the design's "all 6 non-degenerate" expectation, update design.md with a `### Why N instead of 6?` note.

## 8. Docstrings, deviation, and verification gates

- [ ] 8.1 Rewrite the `plotting.py` function docstrings (Google-style; read 2–3 existing circumnutation docstrings first for convention). (The module-docstring `L_gz` drop already landed in the atomic commit, Task 2.3e.) RED (G2): assert no plotting function signature contains an `L_gz`/`growth_zone` parameter (signature introspection), guarding the #230 deviation against regression. Also un-stub the `circumnutation/__init__.py` module docstring line that still calls `plotting` a "remaining stub" (cosmetic, but keeps the record honest).
- [ ] 8.2 Run the full suite + gates: `uv run pytest tests/ -q` (green); `black --check`; `pydocstyle --convention=google sleap_roots/circumnutation/`; `uv lock --check`; `uv run mkdocs build`; `npx openspec validate add-circumnutation-plots --strict`. Aim ≥90% coverage on `plotting.py`; project ≥84% (CI-enforced).
- [ ] 8.3 Reconcile implementation vs proposal/spec/design (every named helper/kwarg/return type matches reality); if any deviation was forced, update proposal.md/spec.md/design.md with a `### Why …?` note and file an issue if a bug/workaround was involved.
